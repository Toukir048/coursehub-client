import { useEffect, useState } from "react";
import { Link } from "react-router";
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import Container from "../../components/shared/Container";
import { getStatistics } from "../../services/dashboard.service";
import type { DashboardStatistics } from "../../types/dashboard";
import { getApiErrorMessage } from "../../utils/getApiErrorMessage";
const Dashboard=()=>{const[data,setData]=useState<DashboardStatistics|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{getStatistics().then((r)=>setData(r.data)).catch((e)=>setError(getApiErrorMessage(e,"Unable to load dashboard statistics."))).finally(()=>setLoading(false));},[]);if(loading)return <div className="flex min-h-[60vh] items-center justify-center"><span className="loading loading-spinner loading-lg"/></div>;if(error||!data)return <main className="py-16"><Container><div className="alert alert-error">{error||"Statistics unavailable."}</div></Container></main>;const cards=[["Total Courses",data.summary.totalCourses],["Total Reviews",data.summary.totalReviews],["Average Price",`৳${data.summary.averagePrice.toFixed(2)}`]];const charts=[{title:"Courses by Category",items:data.categoryData.map((item)=>({label:item.category,total:item.total}))},{title:"Price Ranges",items:data.priceData.map((item)=>({label:item.range,total:item.total}))},{title:"Rating Ranges",items:data.ratingData.map((item)=>({label:item.rating,total:item.total}))}];return <main className="py-16"><Container><h1 className="text-4xl font-bold">Dashboard</h1><div className="mt-8 grid gap-6 md:grid-cols-3">{cards.map(([title,value])=><div key={title} className="rounded-2xl bg-primary p-6 text-primary-content shadow-sm"><p className="opacity-70">{title}</p><p className="mt-2 text-3xl font-bold">{value}</p></div>)}</div><div className="mt-8 grid gap-6 xl:grid-cols-3">{charts.map((chart)=><article key={chart.title} className="rounded-2xl bg-base-100 p-6 shadow-sm"><h2 className="text-xl font-bold">{chart.title}</h2>{chart.items.length?<div className="mt-5 h-72"><ResponsiveContainer width="100%" height="100%"><BarChart data={chart.items}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="label"/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="total" fill="currentColor" className="text-primary"/></BarChart></ResponsiveContainer></div>:<p className="mt-8 text-center opacity-60">No data available.</p>}</article>)}</div><section className="mt-8 rounded-2xl bg-base-100 p-6 shadow-sm"><h2 className="text-2xl font-bold">Recent Courses</h2>{data.recentCourses.length?<div className="mt-5 divide-y">{data.recentCourses.map((course)=><Link key={course._id} to={`/courses/${course._id}`} className="flex flex-wrap justify-between gap-3 py-4"><span className="font-semibold">{course.title}</span><span>{course.category} · ৳{course.price} · ★ {course.rating}</span></Link>)}</div>:<p className="mt-5 opacity-60">No recent courses.</p>}</section></Container></main>;};export default Dashboard;
